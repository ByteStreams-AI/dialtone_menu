# DialTone — marketing site (dialtone.menu, www.dialtone.menu)
# Cloudflare Workers with Static Assets
# Docs: https://developers.cloudflare.com/workers/static-assets/
#
# Static multi-page site (index.html, privacy.html, terms.html). The
# live Worker that serves dialtone.menu was originally created via
# the Cloudflare dashboard's "Upload assets" flow (which generates
# the three-part random name). Keeping that exact name here so
# `wrangler deploy` updates the same Worker rather than creating a
# duplicate, and the existing custom domain bindings
# (`dialtone.menu`, `www.dialtone.menu`) stay intact.

name = "dawn-pine-d058"
main = "worker.js"
compatibility_date = "2026-04-20"
compatibility_flags = []

# Contact form destination. The Worker's `/api/contact` handler sends
# validated submissions via Resend's API; messages are delivered here.
# This is the RECIPIENT only — the sender is still `contact@send.bytestreams.ai`
# on the shared Resend domain (see the RESEND_API_KEY note below). Changing
# this does not change how mail is sent.
# `hello@dialtone.menu` is a Google Workspace mailbox on the DialTone domain,
# so brand mail lands in the brand's own inbox rather than the shared
# ByteStreams one.
[vars]
CONTACT_EMAIL = "hello@dialtone.menu"
# Identifies this site in the contact-form email From display name and
# subject prefix (e.g. `DialTone Contact: <submitter name>`), since
# both DialTone and ByteStreams share one sending domain on Resend
# (`send.bytestreams.ai`). Distinguishing by SITE_NAME keeps inbox
# sorting clean.
SITE_NAME = "DialToneMenu"

# Supabase configuration for form submission persistence.
# `/api/contact` is fail-fast for persistence: if SUPABASE_URL or DB key
# is missing, requests return `503` and do not send email.
SUPABASE_URL = "https://hltmzafywzqajjzjpqva.supabase.co"

# Public menu route (`/m/<slug>`) backend configuration:
# - Reads menu data from Supabase RPC `get_public_menu_by_slug` in the dialtone project.
# - PUBLIC_MENU_SUPABASE_ANON_KEY must be set as a Worker secret (never committed).
#   Run: npx wrangler secret put PUBLIC_MENU_SUPABASE_ANON_KEY
#
# Response behavior for `/m/<slug>`:
# - Friendly `404` page when the slug is unknown/inactive.
# - `503` when the target Supabase project is missing the RPC function
#   (`PGRST202` / function-not-found), indicating environment mismatch.
#
# POINTS AT PRODUCTION as of 2026-08-16 (dialtone#979). It read the STAGING app
# project for months, which was survivable while these hosts served brochure
# menus: an operator's branding and layout choices simply had no effect in prod.
# It stopped being survivable when `<slug>.m.dialtone.menu` became the surface
# every real tenant is served on. A branded host reading staging would take a
# real customer's order into the wrong database, priced against the wrong menu,
# where the prod kitchen never sees it.
#
# The pairing rules that follow from this, each of which fails silently if broken:
#   - PUBLIC_MENU_SUPABASE_ANON_KEY (Worker secret) must be PRODUCTION's anon key.
#     A staging key against this URL is a 401 on every menu — set them together.
#   - STRIPE_PUBLISHABLE_KEY below must be the pk_live_… key, because a charge is
#     minted on the connected account this database names. A pk_test_… key here
#     fails at confirm time, not at load.
#   - Turnstile needs no change: one widget serves both projects and its secret
#     is set on each (dialtone#1201), so moving which database the Worker posts
#     to cannot break siteverify.
#
# [env.preview] still reads STAGING and keeps the pk_test_… key — that is the
# development loop, and the reason this switch does not cost one.
PUBLIC_MENU_SUPABASE_URL = "https://klzznfagrtormretqsgb.supabase.co"

# Cloudflare Turnstile SITE key for the ordering checkout (dialtone#1182 2d).
# A var, not a secret — the site key is public by design and appears in the
# markup of every Turnstile-protected page. The matching SECRET key lives on the
# Supabase Edge Function, which is what verifies the token.
#
# Provisioned 2026-08-16 (dialtone#1201).
#
# ONE widget serves this block AND [env.preview.vars], which is not the "one per
# environment" the issue first assumed. A sitekey and secret are a matched pair,
# and both Workers post their orders to the SAME backend — `PUBLIC_MENU_SUPABASE_URL`
# above is the staging app project in BOTH environments (dialtone#979). Two
# widgets would mint tokens the other environment's secret cannot verify, and
# siteverify would return `invalid-input-response` on every real order.
#
# Registering `dialtone.menu` covers every tenant at `<slug>.m.dialtone.menu`:
# Turnstile applies a hostname to all of its subdomains and rejects wildcards,
# so this stays multi-tenant with no per-slug work. `localhost` is deliberately
# NOT registered — local and CI run in mock mode (no secret), so adding it would
# only let a locally served page mint tokens against the production secret.
#
# #979 LANDED with this change and required NO Turnstile work, which was the
# point of provisioning it this way: the secret was set on BOTH Supabase
# projects, so moving which database this Worker posts to cannot orphan a token.
# Had the widget been per-environment, this switch would have returned
# `invalid-input-response` on every order. Keep both projects' secrets in step —
# a site key here without the secret there collects tokens nobody checks, which
# reads as protection and is not.
TURNSTILE_SITE_KEY = "0x4AAAAAAERQOWbRPlMLVDLj"

# Stripe's PLATFORM publishable key, for the menu's inline checkout
# (dialtone#1182 2e). A var for the same reason as the site key above: a
# publishable key is public by design and appears in the markup of every
# Stripe-powered page. The SECRET key stays on the Supabase Edge Functions.
#
# What it is NOT is the thing that routes money. A charge reaches a particular
# restaurant because the PaymentIntent is minted on THEIR connected account, and
# the checkout gets that account id per-order from `/api/order-payment` — never
# from a build-time value here. So one key serves every tenant on the host.
#
# DELIBERATELY UNSET until the environment is wired, and the two halves agree
# when it is: with no key the checkout says online payment is not set up on this
# menu and points the guest at the phone, rather than mounting a Stripe Element
# that cannot work. Use the pk_test_… key against a staging Supabase and the
# pk_live_… key against production — a mismatch fails at confirm time, not load.
#
# ── App entry host: app.dialtone.menu/r/<slug> (dialtone_app#57) ────────────
#
# The QR target on tables, receipts and the public menu. App installed -> the OS
# opens it at that restaurant; not installed -> a branded page with the stores.
# The guest never types a slug.
#
# All four are DELIBERATELY UNSET, and each degrades honestly while it is:
#
#   APPLE_TEAM_ID        10 chars, developer.apple.com -> Membership details.
#                        The association file 404s until this is a valid team
#                        id. That is on purpose — a file served with a wrong id
#                        verifies against NOTHING and fails silently: links open
#                        the browser, no error on the device, nothing in a log.
#                        A 404 is the honest state and iOS refetches later.
#
#   ANDROID_CERT_SHA256  Play Console -> Setup -> App signing -> the **App
#                        signing key** SHA-256, NOT the upload key. Both are on
#                        that page and look identical. With Play App Signing it
#                        is Google's certificate that signs what users install,
#                        so the upload key fails the same silent way.
#
#   APP_STORE_URL /      Omitted rather than dead-linked while a listing does
#   PLAY_STORE_URL       not exist — a button that goes nowhere is worse than a
#                        button that is absent.
#
# The host also needs a proxied DNS record + a Worker Route, same one-time
# dashboard work as `*.pay` and `*.m` (see developer/production-architecture.md
# in the dialtone repo).
#
APPLE_TEAM_ID = "L33JGV8X8L"
# ANDROID_CERT_SHA256 = "AB:CD:…:EF"
# APP_STORE_URL = "https://apps.apple.com/app/id…"
# PLAY_STORE_URL = "https://play.google.com/store/apps/details?id=com.bytestreams.dialtoneapp"

# Vars do NOT inherit into [env.preview], so set it in BOTH blocks. This one is
# the LIVE key because the block above now reads the production app project —
# the two must move together, and [env.preview] keeps pk_test_… against staging.
STRIPE_PUBLISHABLE_KEY = "pk_live_51TPSsg2KmwBSXLwCNSiNQO7lGGbawvkZRUVLAg7u3TaVeH7q2lCaEBisbziPwDLqtbVdqNSQLsYiPagvVugkYpVa00z3zmCtOB"

# Optional durable rate limiting storage for /api/contact.
# If this binding is configured, `worker.js` will prefer KV-backed rate
# limiting and fall back to in-memory limits if KV is unavailable.
#
# Setup:
# 1) Create namespace: `npx wrangler kv namespace create RATE_LIMIT_KV`
# 2) Replace both placeholder IDs below with the returned IDs.
# 3) Deploy.
[[kv_namespaces]]
binding = "RATE_LIMIT_KV"
id = "40a06e879d7b4cb3aa9752af39cf4de8"
preview_id = "be9cdfc34c7344d4966b2b6b55a02389"

# Required secret (not stored in this file): RESEND_API_KEY
# The `/api/contact` handler uses this to authenticate with Resend's
# API. Set it once per environment via:
#
#   npx wrangler secret put RESEND_API_KEY
#
# The value is encrypted at rest by Cloudflare and exposed to the
# Worker at runtime as `env.RESEND_API_KEY`. Never commit the raw key.
# If the secret is missing, `/api/contact` returns a user-friendly 503
# directing submitters to email `hello@dialtone.menu` manually.

# Required secret (not stored in this file): SUPABASE_KEY
# The `/api/contact` handler uses this to insert waitlist submissions into
# Supabase. Set it via:
#
#   npx wrangler secret put SUPABASE_KEY
#
# Supabase table setup:
# 1. In Supabase console, create table `waitlist_submissions`:
#    CREATE TABLE public.waitlist_submissions (
#      id BIGINT PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
#      email TEXT NOT NULL,
#      name TEXT NOT NULL,
#      restaurant_name TEXT NOT NULL,
#      campaign TEXT NOT NULL DEFAULT 'launch',
#      comment TEXT,
#      created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
#    );
# 2. Enable Row Level Security (RLS) and add policy allowing anonymous inserts
# 3. Get your SUPABASE_URL from Settings → API
# 4. Use an anon-key or service-role key as SUPABASE_KEY
# If SUPABASE_KEY/SUPABASE_SERVICE_ROLE_KEY is missing, `/api/contact`
# returns `503` and no email is sent.

# Assets live under `./public/` so wrangler's dev-server file watcher
# doesn't observe `.wrangler/` writes (which would cause a self-
# triggering reload loop). `public/.assetsignore` excludes the design/
# concept PNGs from being served publicly while still letting them
# live in the repo.
[assets]
directory = "./public/"
# `binding = "ASSETS"` exposes the assets collection to the Worker as
# `env.ASSETS`, which `worker.js` uses to proxy `/favicon.ico` to
# `/images/favicon.svg` and to resolve unmatched paths. Without this
# binding, `env.ASSETS` is undefined and those fallbacks 404 silently.
binding = "ASSETS"
# not_found_handling = "404-page" — this is a static multi-page site,
# not an SPA, so unknown paths should return the branded /404.html
# with a 404 status rather than rewriting to /index.html.
not_found_handling = "404-page"
# With `not_found_handling = "404-page"`, non-asset requests are
# short-circuited to `/404.html` without invoking the Worker. The
# Worker owns these dynamic paths (robots.txt, sitemap.xml, favicon
# fallback to /images/favicon.svg, security.txt, contact form), so
# force them to run through the Worker first instead of being
# treated as missing assets.
# `/` runs the Worker first so a branded menu subdomain (`<slug>.m.dialtone.menu`)
# serves the menu at its root instead of the marketing `index.html` asset. On
# the apex (`dialtone.menu`) the Worker falls through to the assets binding, so
# the homepage is unchanged. See the "Branded menu subdomains" runbook below.
run_worker_first = [
  "/",
  "/robots.txt",
  "/sitemap.xml",
  "/favicon.ico",
  "/.well-known/*",
  "/m/*",
  # Order app (dialtone#1182). MUST be listed: with
  # `not_found_handling = "404-page"` any path that is neither an asset nor on
  # this list is short-circuited to /404.html WITHOUT invoking the Worker — so
  # routing these inside worker.js alone would leave the bundle 404ing while the
  # code looked correct. Same trap `/menu` hit below.
  "/_order/*",
  "/__order-plumbing",
  # App entry host `app.dialtone.menu/r/<slug>` (dialtone_app#57). Third time
  # this trap has been paid for: `/.well-known/*` above was already listed, so
  # the association file worked on the first deploy while `/r/shortys` returned
  # the MARKETING 404 page — the Worker was never invoked. Two paths of one
  # feature, one listed and one not, is the most confusing version of this.
  "/r/*",
  # `/menu` is the stable menu URL on a branded subdomain (#986 Phase 2). It
  # MUST be listed here: with `not_found_handling = "404-page"`, any path that
  # is neither an asset nor on this list is short-circuited to /404.html
  # WITHOUT invoking the Worker — so routing it inside worker.js alone left
  # `<slug>.m.dialtone.menu/menu` serving the marketing 404 page. Worker tests
  # cannot catch that: they call worker.fetch() directly, below the layer that
  # does the short-circuiting.
  "/menu",
  "/menu/",
  "/api/*"
]

# ── Branded menu hosts (`<slug>.m.dialtone.menu`) ───────────────────────────
# The Worker serves each restaurant's menu at its own host (the path form
# `dialtone.menu/m/<slug>` still works). `extractMenuSlugFromHost` in worker.js
# maps `<slug>.m.dialtone.menu` → that restaurant's menu.
#
# WHY `.m.` AND NOT `<slug>.dialtone.menu` — READ BEFORE "SIMPLIFYING" THIS.
# Serving slugs at the first level needs the route `*.dialtone.menu/*`, and that
# route matched EVERY app host: admin, kitchen, beverage, expo and their
# -staging variants all served the marketing site in production until it was
# deleted (dialtone#1011). The assumption it was added on — "Custom Domains
# out-rank a wildcard Route" — is false. Scoping one level deeper cannot overlap
# a single-label app host, which is why `*.pay.dialtone.menu` has always been
# safe. The alternative, a Custom Domain per restaurant, caps at 100 per zone
# and adds a provisioning step to every onboarding.
#
# There is no reserved-subdomain list any more: nothing but restaurants lives
# under `.m.`, so a restaurant legitimately called "The Kitchen" can have
# `kitchen.m.dialtone.menu`.
#
# Three one-time DASHBOARD steps (not managed by wrangler, so the existing
# `dialtone.menu` / `www` Custom Domains aren't disturbed):
#   1. TLS FIRST: `*.m.dialtone.menu` is a SECOND-level wildcard, which
#      Universal SSL does NOT cover — order an ACM/advanced certificate for it,
#      exactly as `*.pay.dialtone.menu` needed. Do this first; it takes minutes
#      to validate and without it every branded host fails the handshake.
#   2. DNS: add a PROXIED (orange-cloud) record `*.m` in the dialtone.menu zone
#      (AAAA `*.m` → `100::` proxied is the conventional placeholder).
#   3. Route: Workers & Pages → this Worker (`dawn-pine-d058`) → Triggers →
#      add Route `*.m.dialtone.menu/*` (zone dialtone.menu).
#
# VERIFY BY CONTENT, NOT STATUS CODE. The outage above passed a 200-only check:
# every app host returned 200 while serving the marketing homepage. Fetch each
# host and assert what came back, and include the -staging hosts:
#   for h in admin kitchen beverage expo admin-staging kitchen-staging \
#            beverage-staging; do
#     printf '%s -> ' "$h"
#     curl -s "https://$h.dialtone.menu/" | grep -o '<title>[^<]*' | head -1
#   done
# Then: `https://<any-live-slug>.m.dialtone.menu/` renders that menu, and an
# unprovisioned slug returns the friendly menu-not-found page.

# Declares the secret names required by this Worker.
# `wrangler deploy` and `wrangler dev` will warn if any are absent.
# These are *names only* — values are never stored here.
# Upload values with: bash developer/ci/deploy-prod-secrets.sh
[secrets]
required = [
  "RESEND_API_KEY",
  "SUPABASE_SERVICE_ROLE_KEY",
  "SUPABASE_KEY"
]

# Keep Worker observability settings in source control so dashboard and
# CI/CD deployments stay consistent.
[observability.logs]
enabled = true
invocation_logs = true

[observability.traces]
enabled = false

# ── Preview environment (dialtone#1183) ────────────────────────────────────
# A SECOND Worker, on a free `*.workers.dev` host, so a branch can be deployed
# without touching production. Until this existed there was exactly one target
# and it was the live site: every merge to `main` deployed straight to prod, and
# the only non-deploy loop was `dev/preview-menu.mjs`, which renders template
# modules but cannot exercise worker ROUTING or a payment flow.
#
# That was survivable while this Worker served brochure pages. It is not
# survivable for web ordering (dialtone#1182), which adds a service binding,
# proxied asset paths and eventually a Stripe Payment Element — none of which
# can be built deploy-to-find-out on the page that takes money.
#
# DELIBERATELY NO CUSTOM DOMAIN. `workers.dev` needs no ACM certificate, no
# proxied DNS record and no Worker route — which is the expensive, fiddly part
# of the branded-host setup and the reason `*.pay` and `*.m` each took real
# effort. The cost of this environment is therefore a config block and a
# workflow, not a day of dashboard work.
#
# WHAT PREVIEW CANNOT TEST: host-based slug extraction
# (`<slug>.m.dialtone.menu` → `extractMenuSlugFromHost`), because a workers.dev
# host has no slug in it. Use the PATH form `/m/<slug>`, which is retained and
# works identically below the host-parsing layer. Host routing is already live,
# already guarded by the content-verify step in deploy.yml, and unchanged by
# #1182 — so it is not on the critical path. If it ever changes, THAT is when to
# buy a `*.m-preview` certificate.
#
# WRANGLER GOTCHA — ENVIRONMENTS DO NOT INHERIT MOST CONFIG. `main` and
# `compatibility_date` are inherited; `vars`, `kv_namespaces`, `assets` and
# `observability` are NOT. Everything below is restated on purpose. Omitting a
# var here does not fall back to the top-level value — it is simply absent at
# runtime, and the failure shows up as a 503 from a handler that looks fine in
# the diff. When you change a top-level var, change it here too.
#
# SECRETS ARE PER-ENVIRONMENT and must be uploaded separately:
#   npx wrangler secret put PUBLIC_MENU_SUPABASE_ANON_KEY --env preview
#   npx wrangler secret put SUPABASE_KEY --env preview          # contact form
#   npx wrangler secret put RESEND_API_KEY --env preview        # contact form
# Only the first is needed for menus. Without the other two `/api/contact`
# returns 503 on preview, which is correct — preview should not be emailing.
[env.preview]
name = "dialtone-menu-preview"
workers_dev = true

[env.preview.vars]
CONTACT_EMAIL = "hello@dialtone.menu"
SITE_NAME = "DialToneMenu (preview)"
SUPABASE_URL = "https://hltmzafywzqajjzjpqva.supabase.co"
# Same project the production Worker reads today — the public menu is served
# from the STAGING app database by deliberate decision (dialtone#979). Preview
# changes WHERE THE CODE RUNS, not which data it reads, which is what keeps
# this environment cheap. Flipping menus to the prod app project is #979 and is
# a launch gate for a real paying tenant, not a build gate.
PUBLIC_MENU_SUPABASE_URL = "https://mxhyvvgjtqllohpvrwon.supabase.co"
# The TEST key, because preview reads the staging app project (above). See the
# top-level [vars] block for what this key does and does not route.
STRIPE_PUBLISHABLE_KEY = "pk_test_51TUZtORrmddnOdHkrUR2E2FSE00Z4FD0n5is4aGTeIdfB8hdFcg9UcFn33sEJITrKrEkUWR1RuHcBk13LvceCdAb00g41bVj85"
# The SAME widget as the top-level [vars] block, not a preview-specific one:
# preview posts its orders to the staging app project above, which holds the one
# secret that can verify these tokens. Vars do NOT inherit, so it is restated
# here — omit it and preview renders no widget, sends no token, and every order
# is refused `challenge_failed` once the secret is set. The preview host is
# registered on the widget for the same reason.
TURNSTILE_SITE_KEY = "0x4AAAAAAERQOWbRPlMLVDLj"

[[env.preview.kv_namespaces]]
binding = "RATE_LIMIT_KV"
# The preview namespace that already existed as `preview_id` on the production
# binding — so preview rate-limiting cannot evict production's entries.
id = "be9cdfc34c7344d4966b2b6b55a02389"

[env.preview.assets]
directory = "./public/"
binding = "ASSETS"
not_found_handling = "404-page"
run_worker_first = [
  "/",
  "/robots.txt",
  "/sitemap.xml",
  "/favicon.ico",
  "/.well-known/*",
  "/m/*",
  "/_order/*",
  "/__order-plumbing",
  "/r/*",
  "/menu",
  "/menu/",
  "/api/*"
]

[env.preview.observability.logs]
enabled = true
invocation_logs = true

# Preview requires ONLY the menu key. Wrangler warns when `[secrets]` exists at
# the top level but not here, because it is not inherited either — and the
# narrower list is deliberate, not an oversight: RESEND_API_KEY and SUPABASE_KEY
# drive `/api/contact`, and a preview environment has no business sending email
# or writing waitlist rows. Without them that route returns 503, which is the
# correct behaviour here.
[env.preview.secrets]
required = [
  "PUBLIC_MENU_SUPABASE_ANON_KEY"
]

# ── Order app service binding (dialtone#1182 Phase 2b) ──────────────────────
#
# The cart is a script INJECTED into the page this Worker already rendered, and
# it is served through this binding. So an unbound environment cannot render
# ordering AT ALL — `worker.js` returns a 503 naming the binding, and the page
# comes back with no cart for every tenant, regardless of `web_ordering_enabled`.
# That is precisely what `*.m.dialtone.menu` did until now: zero `dt-order`
# markers on both tenants, while preview served 40 for the same restaurant.
#
# This was preview-only on the stated grounds that a binding names a Worker that
# must already exist, and `dialtone-order` had not been deployed to production.
# It has been since 2026-08-15 ("Deploy order (production): success"), so the
# reason expired before the comment did.
[[services]]
binding = "ORDER_APP"
service = "dialtone-order"

[[env.preview.services]]
binding = "ORDER_APP"
service = "dialtone-order-staging"
